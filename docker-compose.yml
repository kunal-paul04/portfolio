version: "3.9"

services:
  app:
    build: .
    ports:
      - "8008:8008"
    env_file:
      - .env
    volumes:
      - .:/app   # 👈 REQUIRED for reload
    command: >
      uvicorn app.main:app
      --host 0.0.0.0
      --port 8008
      --reload